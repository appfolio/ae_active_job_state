name: Release Gem

on:
  push:
    branches:
      - main
      - master
    paths:
      - 'ae_active_job_state.gemspec'
      - 'lib/**'

permissions:
  contents: write
  pull-requests: write

jobs:
  release:
    uses: appfolio/rubygems-releaser/.github/workflows/release.yml@v1
